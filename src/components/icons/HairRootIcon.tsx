type HairRootIconProps = {
  className?: string;
};

export default function HairRootIcon({ className }: HairRootIconProps) {
  return (
    <img
      src="/icons/hair-root-skin-icon (copy).svg"
      alt=""
      aria-hidden="true"
      className={className}
    />
  );
}